# 2018.8.1
1.完成AngularJs基础查看
2.复习昨日的Angular
3.AngularJs写的小demo(笔记本动态显示当前字数，并且不能超过100个字)
4.前端demo页面的构建

# 2018.8.2
1.完成接口的路由及传参
  小问题(1)接口被拦截，通过增加路径/api解决
       (2)接口传参方式，利用实体，将参数传到后台
       (3)接口保存时报错，问题出在实体类，将主键自增那块不要映射，不然会报错。
2.完成分页功能（原来都是拿原生的写的）
3.明天写下分页查询

# 2018.8.3
1.编辑操作
 (1)jpa这边快把老子卡死掉了，不习惯真的觉得难用到死
 (2)修改时加 @params
2.查询操作
 (1)特别简陋的写了一个，一会儿打算再写一个精简版的list
 
 # 2018.8.6
 1.第一个双休结束
 2.清晨，雨，公司里还没什么人，起太早头疼也有点困
 3.今天会有新项目做，试试吧
 4.知识图谱修改后台显示及查询(还是显示之前的MindNode)
 5.新框架搭建(pom.xml)
 6.数据连接池配置，Aop配置(启动类不可以跟其他包在同一级下，要不然会扫描不到)
 
 # 2018.8.7
 1.简单的保存操作（新框架测试）
 2.controller层Restful风格(试用)，前端AngularJS
 3.@RequestMapping和@GetMapping,@PostMapping(好多人不讲原理，谁还不会用嘛)
 4.AngularJs在新框架的试用（测试成功），打算跳转什么的用路由，文件上传的还是AJAX(或许到时候调整新方案)
 5.最近开始根据业务设计后台的表结构（用户表，权限表，上传审核文件）
 6.界面先用简陋的风格写（回头加ui）
 
 # 2018.8.8
 1.毛线投中配置Redis(Jedis)
 2.本地配置Redis服务
 3.设计数据库表结构(不用外键关联很开心)
 4.注册接口编写
 5.为了统一规范，将原来的Hibernate换成JPA了,看来这个JPA是喜欢我喜欢的不得了呢
 6.前端用写注册接口
 
 # 2018.8.9
 1.注册接口编写
 2.现有代码统一用ResultBean接收
 3.增加验证码接口(通过全局变量记录验证码),密码MD5加密
 4.登录接口的编写(密码MD5加密后验证)    //前端AngularJS路由时，模块注入错误，其实是controller不匹配(大小写错了)
 5.退出登录接口
 6.文字识别百度Ai和高光仪识别
 
 # 2018.8.10
 1.审核表Auditiing创建
 2.审核表Repository层创建及Service层创建
 3.审核模块流程图规划(分为客户端和管理端两部分，客户端首次填写时，依次单张上传图片，后边显示审核状态，再次登录时，根据函数判断是否通过审核，未通
   过审核继续在该页面将未审核通过的资料编辑)，管理员端以分页的形式下拉未审核的用户，点击审核，弹窗，在弹窗里进一步审核，弹窗内可显示图片，审核后
   统一点击提交按钮提交
 4.高拍仪扫描文件(2h)
 
 # 2018.8.13
 1.上传模块测试
 2.登录模块、审核模块对接
 3.实体类补充real_path,后续更新删除用
 4.上传模块补充文件类型选择器，根据不同类型文件上传到不同文件夹(上传文件夹以个人用户为单元，内分为5个内容，对应不同类型的上传文件)
 5.上传文件模块，实时更新数据和文件内容
 6.前端实时反馈文件处理状态
 
 # 2018.8.14
 1.审判检测接口（实时监测检测状态，并给出相应的反馈）
 2.动态赋值函数（实时显示数据），感觉status没啥用了现在
 3.检测审核接口后台，主要是返回list,message
 4.excel表格解析数据
 
 # 2018.8.15
 1.博客一篇(总结)
 2.excel解析出的json文件，能够正常读取
   从jsonArray中取出，转换为map,entrySet遍历map,读取出相应的key值，val值
 3.创建银行账单表Ticket(English is so hard,a little 233333)
 4.resultBean用起来好恶心，略略略(dao层 & service层)
   jpaRepository可以调page也可以调findall,还可以saveAndFlush（总算齐活儿了）
 5.简单的账单上传页面以及通信测试
 
 # 2018.8.16
 1.先祝自己生日快乐，能写更好的代码，有更好的习惯，学到更多的东西
 2.解决file与multipartfile转换的问题（还是直接写文件比较好）
 3.上传文件后解析传入数据库中
 4.文件中针对银行模板的解析模块(不以银行账号为主键,防止异常出现时，后续数据无法正常导入,普通过滤器)
 5.根据判断创造的特征值，保证数据的完整性
 
 # 2018.8.17
 1.票据分页界面显示
 2.票据文件名限制
 3.票据上传页面
 4.复杂查询分页测试(jpa学一学还是好用的，啊啊啊啊啊累)
 5.票据查询界面查询界面及接口
 
 # 2018.8.19
 1.电脑清理
 2.技术栈(数据结构与算法，树的遍历)
 
 # 2018.8.20
 1.周一，想要做的事，查询功能（复杂查询，查询后自动分页）
   jpa中用的还是JpaSpecificationExecutor接口，通过判断为不为空，动态生成语句，执行查询（之前的没错，只是一页只显示了一行）
 2.查询后，显示查询后的分页(多一句嘴，AngularJS真好用~~~~~~~~~~~~真香)
 3.将之前代码中不规范的(比如controller中调repository)部分，修改
 4.查询时，默认类型为全部
 5.银行列表（实体类创建），excel导入模板的创建及数据的导入
 6.交易表Trade的实体类的创建,确定其数据类型(日期的数据类型为YY-MM-DD)
   默认Data的通过ORM映射在数据库表中的是dataTime(YY-MM-DD hh:mm:ss),这时候通过之前的老方法规定他的生成类型columnDefinition
 7.修改 解析excel文件时的时间数据，由原来的String类型更改为Date   
 8.二叉树的学习（看不懂的部分是二叉树的结构，也就是不会用）
 
 # 2018.8.21
 1.构建交易模态框
 2.通过前台时间戳获取当前日期  AngularJs时间戳转换
  e.g   到期时间:{{maturityTime | date:'yyyy-MM-dd'}}<br>
 3.调整查询结构，票据类型和票据名称互换
 4.票据日期的计算，过期票据直接抛出
 5.封装预估利率函数（精度用BigDecimal保持在2位）
 6.银行的Repository及Service层的创建，以及构建根据名称查询实体类
 7.构建交易类Trade的Repostiory层,及Servic，Imple层
 8.trade(交易表)Repostiory层的 创建，以及相关查询方法的构建
 9.过滤导入excel的数据，一个是银行，一个是小数点
 10.数据库中float的映射
 
 # 2018.8.22
 1.预估价值模块完成并测试
 2.树的学习
 3.百度OCR自动识别表格（异步识别）封装
 4.博客撰写
 
 # 2018.8.23
 1.数据结构堆的学习
 2.一道算法题(用堆理解还是很难，再看看啦)
 3.创建产品表product(用于预估收益)
 4.创建票据状态表ticketStatus
 
 # 2018.8.24
 1.商量并修改银行表(源数据有问题)，过滤掉银行后面的字段，扩大信息容量
 2.计算各种理财产品的预估收益
 3.封装根据产品获取理财收益的函数
 
 # 2018.8.27
 1.表格数据显示，先把表格设计好（设计完成，换个域，将ng-repeat放在了tbody里）
 2.加权限，具体与当前登录的用户有关(之前脑子是抽了吗)
 3.修改原来的获取票据数据的方法，根据当前登录的用户获取该用户的数据，如果未登录，将跳转至登录界面
 4.学校材料准备
 5.票据贴现界面的构建
 
 # 2018.8.28
 1.票据贴现界面，数据存储部分
 2.票据贴现，显示日期最新的数据
 3.数据结构
 4.材料盖章
